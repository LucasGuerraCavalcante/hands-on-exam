// 2. Write a sorting algorithm to organize an array of numbers in a decreasing order. (1 point)

// Nao tive tempo para implementar o meu proprio algoritimo :C 

const sortItems = (array) => {
	return array.sort().reverse();
}

console.log(sortItems([2, 5, 2, 9]))
console.log(sortItems([4, 7, 8, 2, 0, -5, 9]))
